services:
  bot:
    build: .
    env_file:
      - .env
    ports:
      - "${METRICS_BIND_IP:-127.0.0.1}:${METRICS_PORT:-9090}:9090"
    volumes:
      - ./cookies.txt:/app/cookies.txt
    restart: unless-stopped
